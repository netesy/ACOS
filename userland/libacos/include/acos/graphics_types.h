#pragma once
#include <acos/types.h>
#include <acos/abi/graphics.h>

namespace acos::graphics {

using Color = abi::Color;
using Gradient = abi::Gradient;
using ClipRect = abi::ClipRect;

} // namespace acos::graphics
